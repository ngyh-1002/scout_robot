---

## 📝 Nav2 파라미터 변경 사항 (README.md)

### 📌 프로젝트: 로컬 경로 계획 실패 (Collision Ahead) 수정

이 문서는 DWB Local Planner에서 발생한 `[ERROR] [DWBLocalPlanner]: No valid trajectories` 및 `[WARN] [behavior_server]: Collision Ahead` 오류를 해결하기 위해 `nav2_params.yaml` 파일에서 변경된 주요 설정들을 요약합니다.

### 1. 로봇 발자국 및 크기 정의 변경 (충돌 감지 정확도 향상)

기존 설정은 로봇을 작은 원형(반지름 0.22m)으로 가정했으나, 실제 로봇 크기(앞뒤 0.62m, 좌우 0.58m)에 맞춰 **다각형 발자국(`footprint`)**을 정의했습니다.

| 섹션 | 파라미터 | 변경 내용 | 기능 및 목적 |
| :--- | :--- | :--- | :--- |
| `local_costmap` / `global_costmap` | `robot_radius` | 0.22에서 **주석 처리/제거** | 로봇이 원형이 아니므로, 원형 기반 충돌 계산을 비활성화합니다. |
| `local_costmap` / `global_costmap` | **`footprint`** | **새로운 다각형 좌표 추가** (`[[0.31, 0.29], [0.31, -0.29], [-0.31, -0.29], [-0.31, 0.29]]`) | 로봇의 실제 직사각형 형태(0.62m x 0.58m)를 코스트맵에 정확히 정의하여, **실제 로봇 모양에 기반한 충돌 감지**를 수행하도록 합니다. |

---

### 2. 코스트맵 팽창 반경 조정 (경로 확보)

로봇 발자국이 커지고 DWB 오류가 발생했기 때문에, 장애물 주변의 '경계 구역'을 줄여 로봇이 움직일 수 있는 공간을 확보했습니다.

| 섹션 | 파라미터 | 변경 전 | 변경 후 (예시) | 기능 및 목적 |
| :--- | :--- | :--- | :--- | :--- |
| `local_costmap`/`inflation_layer` | **`inflation_radius`** | 0.55 | **0.40** (또는 0.35 등) | 장애물 주변에 코스트를 부과하는 **최대 반경**을 줄입니다. 이로써 로봇이 좁은 통로를 통과할 때, 전체 공간이 이동 불가능한(Fatal) 영역으로 간주되는 것을 방지합니다. **DWB 오류 해결에 핵심적인 역할**을 합니다. |

---

### 3. 시뮬레이션 시간 설정 통일 (TF 동기화)

사용자가 `launch` 파일에서 `use_sim_time:=false`를 사용했으므로, YAML 파일의 설정도 실제 시간(`false`)을 사용하도록 통일했습니다. **모든 `use_sim_time: True` 설정**을 수정해야 합니다.

| 섹션 | 파라미터 | 변경 전 | 변경 후 | 기능 및 목적 |
| :--- | :--- | :--- | :--- | :--- |
| `amcl`, `bt_navigator`, `controller_server`, `local_costmap`, `global_costmap`, `planner_server`, `behavior_server` 등 **총 11곳** | **`use_sim_time`** | True | **False** | Nav2 스택의 모든 노드가 **시뮬레이션 시간(ROS Time)**이 아닌 **실제 시스템 시간(Wall Time)**을 사용하도록 통일합니다. 이는 오도메트리/Lidar 데이터와 TF(Transform) 간의 **시간 동기화 오류**를 방지합니다. |

---

### 4. DWBLocalPlanner 장애물 평가 민감도 조정 (선택적)

`inflation_radius` 및 `footprint` 수정 후에도 오류가 지속될 경우를 대비하여 DWB가 장애물을 평가하는 민감도를 조정할 수 있습니다.

| 섹션 | 파라미터 | 변경 전 | 변경 후 (예시) | 기능 및 목적 |
| :--- | :--- | :--- | :--- | :--- |
| `controller_server`/`FollowPath` | **`BaseObstacle.scale`** | 0.02 | **0.015** (등으로 감소) | DWB가 생성한 궤적의 장애물 충돌 임계치에 대한 **가중치(Weight)**입니다. 이 값을 줄이면 장애물에 조금 더 가까운 궤적도 유효하다고 판단하게 되어 **경로 생성 성공률**을 높입니다. |

### 🚨 중요 공지: 파라미터 파일 위치

이 설정 파일은 시스템 설치 디렉토리인 **`/opt/ros/humble/share/nav2_bringup/params`** 내에서 직접 수정되었습니다. 이는 시스템 업데이트 시 변경 사항이 초기화되거나 덮어씌워질 수 있으므로, **추후에는 워크스페이스 내로 파일을 복사하여 사용하는 것을 강력히 권장**합니다.
