---

# README — DWB vs MPPI 로컬 플래너 비교

## 📌 개요

scout_mini를 ROS2 Humble + Nav2 공식 `nav2_bringup` 환경에서 운용할 때, 로컬 플래너는 **DWB(Dynamic Window Based Controller)** 와 **MPPI(Model Predictive Path Integral Control)** 중 선택할 수 있다.

두 알고리즘은 모두 “미래 궤적을 시뮬레이션하여 최적의 제어 명령을 선택한다”는 공통점이 있지만, **탐색 방식·속도 모델·계산량·주행 특성**에서 큰 차이가 있다.

---

# 🔥 1. DWB와 MPPI의 핵심 차이점

## ▶ DWB: 단일 속도 조합 기반의 결정적 탐색

* 일정한 선속도(v), 각속도(w) 후보들을 생성
* 후보 속도 **하나**로 궤적 전체를 시뮬레이션함
* critic 기반 점수 계산 후 최고 점수 속도 하나를 선택
* 빠르고 단순하지만 궤적 유연성이 떨어짐
* CPU 사용량이 매우 적음 (Raspberry Pi 사용 가능)

### **핵심 특징 요약**

* 🟦 “**미래 1초를 단일 속도로 쭉 가본다**”
* 🟦 정해진 후보 속도 집합만 탐색 → 제한적
* 🟦 critic 기반 deterministic scoring

---

## ▶ MPPI: 시간에 따라 변하는 속도 시퀀스 기반 확률적 탐색

* 속도 시퀀스(각 타임스텝마다 v,w가 달라지는 형태)를 수백~수천 개 랜덤 샘플링
* 각 시퀀스마다 궤적을 시뮬레이션
* 비용 기반 가중치로 최적 방향을 확률적으로 선택
* 부드럽고 유연하며 장애물 회피 성능이 매우 뛰어남
* CPU 자원이 필요함 (라즈베리파이에서는 부담 큼)

### **핵심 특징 요약**

* 🔴 “**미래 N스텝의 속도 변화를 모두 샘플링해본다**”
* 🔴 다양한 곡률·방향·속도 변화 가능
* 🔴 stochastic optimization 기반

---

# 🔧 2. CPU 문제로 DWB를 사용할 수밖에 없었던 이유

MPPI는 다음 동작 때문에 CPU 사용량이 급증한다:

1. **수백~수천 개의 속도 시퀀스 샘플 생성**
2. **각 시퀀스마다 10~30개의 time-step을 시뮬레이션**
3. **각 스텝에서 costmap 비용 평가**
4. **확률적 weighting 연산 수행**

라즈베리파이4(특히 Nav2 + AMCL + Costmap 서버 동시 실행 환경)에서는:

* MPPI의 기본 샘플 수(200~1000)
* 매 cycle마다 실행되는 연산량
* costmap과 tf 연산까지 추가

이 모든 것이 누적되면 **cycle miss**가 발생하고 컨트롤 루프가 불안해짐.

반면 DWB는:

* 고정된 속도 후보(보통 30~80개)
* 단일 속도로 짧은 궤적만 생성
* critic 연산만 수행

이라서 **라즈베리파이 환경에서도 20~30Hz 컨트롤이 안정적으로 유지됨**.

따라서 **CPU 여유가 없는 환경에서는 DWB가 사실상 필수 선택**이다.

---

# 📊 3. DWB vs MPPI 장단점 요약 표

| 항목       | **DWB**                     | **MPPI**               |
| -------- | --------------------------- | ---------------------- |
| 궤적 생성 방식 | 단일 속도 조합(v,w)로 일정한 궤적       | 시간에 따라 변하는 속도 시퀀스      |
| 탐색 방식    | 결정적(deterministic), grid 탐색 | 확률적(stochastic), 샘플 기반 |
| 궤적 유연성   | 낮음                          | 매우 높음           |
| 부드러운 주행  | 중간                          | 매우 우수                  |
| 장애물 회피   | 제한적 (고정 후보만 탐색)             | 매우 뛰어남 (다양한 가능성 탐색)    |
| 경로 추종    | 흔들림·지그재그 가능                 | 부드럽고 안정적               |
| 계산량      | 매우 적음 → RPi4 가능             | 매우 큼 → 고성능 CPU 필요(라즈베리파이4기준으로 튜닝할경우 샘플수를 줄여야하기에 dwb와 성능이 떨어질가능성 높음)   |
| 안정성      | 가벼움·안정적                     | cycle miss 발생 위험       |
| 튜닝 난이도   | 쉬움                          | 어려움(매개변수 많음)           |
| 추천 환경    | 소형 로봇, 라즈베리파이               | 산업용 PC, 고성능 onboard    |

---

## ✔ 최종 결론 요약

```
## ✔ 최종 결론 요약

```
DWB = 고정된 속도의 짧은경로를 수백개 생성해 단순하지만 안정적인 플래너. 계산량이 매우적어 CPU무리없이 가볍고 안정적이며, 튜닝난이도 쉬움 
MPPI = 변하는 속도의 긴경로를 수천개 생성해 부드럽고 유연한 플래너. 계산량이 매우 커 고성능 CPU가 필요함, 라즈베리 파이4에서 튜닝해 사용할 경우 복잡한 튜닝을 하더라도, DWB와 성능차이가 없을 가능성 높음
```

라즈베리파이4에서는 CPU 한계 때문에
**DWB가 안정적인 선택**이고,
MPPI는 **고성능 컴퓨터나 Xavier/Orin급**에서 사용해야 효과를 본다.

---
