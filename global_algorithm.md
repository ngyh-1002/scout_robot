---

# README — A* 알고리즘 vs Dijkstra 알고리즘 비교

## 📌 개요

Nav2(Global Planner, NavFn/Smac 등)에서는 전역 경로 탐색 시
**Dijkstra** 또는 **A*** 알고리즘을 선택할 수 있다.

두 알고리즘 모두 **그래프(그리드맵)에서 최단 경로를 찾는 탐색 알고리즘**이지만,
탐색 방식과 효율성(CPU 연산량)에서 중요한 차이를 가진다.

---

# 🔥 1. Dijkstra 알고리즘

## ▶ 특징

* 시작점에서 모든 방향으로 동일하게 비용을 확장하는 방식 (균일 비용 탐색)
* 목표지점까지의 “거리 정보” 없이 맵 전체를 탐색할 수 있음
* 항상 최단경로를 찾지만, **탐색량이 매우 큼**
* 휴리스틱을 사용하지 않음

### 핵심 요약

* 🟦 “**갈 수 있는 모든 노드를 일정 비용으로 확장한다**”
* 🟦 맵이 크면 클수록 탐색 비용 증가
* 🟦 CPU 연산량이 큼
* 🟦 휴리스틱 없음

---

# 🔥 2. A* 알고리즘 (A-Star)

## ▶ 특징

* Dijkstra에 **휴리스틱(Heuristic)** 을 추가한 강화 버전

* 탐색 평가식

  ```
  f(n) = g(n) + h(n)
  ```

  * g(n): 실제로 이동한 비용
  * h(n): 목표까지의 예측 비용(휴리스틱)

* 일반적으로 Nav2에서는 **유클리드 거리(Euclidean distance)** 를 휴리스틱으로 사용

  ```
  h(n) = sqrt( (x2 - x1)^2 + (y2 - y1)^2 )
  ```

* 휴리스틱 덕분에 목표 방향으로 탐색을 집중 → **CPU 연산량이 훨씬 적음**

* 항상 최단경로를 보장 (적절한 휴리스틱일 경우 Admissible)

### 핵심 요약

* 🔴 “**목표 방향으로 갈 확률이 높은 노드만 우선적으로 탐색한다**”
* 🔴 CPU 연산량이 Dijkstra 대비 크게 감소
* 🔴 휴리스틱(Euclidean Distance) 사용
* 🔴 최단경로 보장

---

# ⚙ CPU 성능 측면 비교

| 알고리즘         | CPU 사용량 | 이유                            |
| ------------ | ------- | ----------------------------- |
| **Dijkstra** | 높음      | 목표 방향 정보가 없어 맵 전체를 균일하게 탐색    |
| **A***       | 낮음      | 유클리드 휴리스틱으로 목표 방향 중심으로 탐색량 감소 |

→ **같은 맵에서 A*는 Dijkstra보다 훨씬 적은 노드를 탐색하므로 CPU 사용량이 적다.**

---

# 📊 A* vs Dijkstra 비교 요약

| 기준         | **Dijkstra** | **A***          |
| ---------- | ------------ | --------------- |
| 휴리스틱       | 사용 안 함       | 사용함(Euclidean)  |
| 탐색 범위      | 전체맵 균일 탐색    | 목표 방향 중심 탐색     |
| CPU 효율     | 낮음(연산량 많음)   | 높음(연산량 적음)      |
| 최단 경로      | 보장           | 보장              |
| 알고리즘 특징    | 단순하지만 비효율적   | Dijkstra + 휴리스틱 |
| Nav2에서 선호도 | 낮음           | 매우 높음(기본 선택)    |

---

# ✔ 최종 결론 요약
<img width="3176" height="1799" alt="스크린샷 2025-11-15 14-18-31" src="https://github.com/user-attachments/assets/b0a33692-1b34-4d70-8a8c-41dba560ceca" />

```
A* = 휴리스틱(Euclidean Distance)을 사용하여 목표 방향으로 탐색을 집중시키므로  
     Dijkstra보다 CPU 연산량이 훨씬 적고, Nav2에서 기본적으로 선호됨.

Dijkstra = 모든 방향을 동일하게 확장하는 균일 비용 탐색으로  
           정확하지만 탐색량이 많아 CPU 부담이 큼.
```
